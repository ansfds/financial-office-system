import {cookies} from 'next/headers';import {db} from '@/lib/db';import {COOKIE,audit,unpack} from '@/lib/auth';import {ok} from '@/lib/http';export async function POST(){const c=await cookies(),id=unpack(c.get(COOKIE)?.value);if(id)await db.loginSession.updateMany({where:{id},data:{revokedAt:new Date()}});await audit('LOGOUT',{description:'تسجيل الخروج'});c.delete(COOKIE);return ok({success:true})}
