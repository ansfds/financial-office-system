import Page from '@/components/Page';import {db} from '@/lib/db';export const dynamic = 'force-dynamic';export const revalidate = 0;export default async function P(){const xs=await db.auditLog.findMany({orderBy:{createdAt:'desc'},take:300});return <Page title="سجل التعديلات"><div className="table-wrap"><table><thead><tr><th>العملية</th><th>الوصف</th><th>القسم</th><th>IP</th><th>التاريخ</th></tr></thead><tbody>{xs.map(x=><tr key={x.id}><td>{x.action}</td><td>{x.description||'—'}</td><td>{x.entityType||'—'}</td><td>{x.ip||'—'}</td><td>{x.createdAt.toLocaleString('ar')}</td></tr>)}</tbody></table></div></Page>}
